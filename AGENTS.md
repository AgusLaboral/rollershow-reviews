# AGENTS.md — espejo (no editar acá)

**La fuente de verdad de este proyecto es `CLAUDE.md`, en esta misma carpeta. Leelo AHORA, antes de hacer cualquier cosa.** Contiene: qué es el proyecto, por qué está aislado de `rollershow` y `rollershow-src` (no mezclar), stack (single-file vanilla, sin build), cómo correr las verificaciones con Playwright, cómo deployar (GitHub Pages, repo `AgusLaboral/rollershow-reviews`), decisiones ya cerradas que no hay que re-litigar, y los pendientes reales (`TODO(Nico)` en `index.html`).

Este archivo existe solo para que Codex y Claude Code arranquen con las mismas instrucciones. No agregar reglas acá: editar `CLAUDE.md`. Si este archivo contradice a `CLAUDE.md`, este archivo está desactualizado.
